{
    "name": "photography-portfolio-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm install && cd frontend && npm install && npm run build",
        "install-backend": "cd backend && npm install",
        "build-all": "npm run install-backend && npm run build",
        "deploy-backend": "cd backend && npx wrangler deploy",
        "cf-pages-dev": "cd frontend && npm start"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.38.4",
        "axios": "^1.13.2",
        "bcryptjs": "^2.4.3",
        "compression": "^1.8.1",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "express-rate-limit": "^6.10.0",
        "imagekit": "^6.0.0",
        "jsonwebtoken": "^9.0.2",
        "nodemailer": "^7.0.6",
        "pg": "^8.11.3",
        "serverless-http": "^4.0.0"
    }
}